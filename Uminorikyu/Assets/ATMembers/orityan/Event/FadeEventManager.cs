using UnityEngine;

public class FadeEventManager : MonoBehaviour
{
    public bool isFading;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        isFading = true;
    }
}
